# ─── Shared config (loaded for all modes) ───
# Usage:
#   npm run dev:devnet   →  loads .env + .env.devnet
#   npm run dev:mainnet  →  loads .env + .env.mainnet
#   npm run build:mainnet →  production build for mainnet

# Jupiter API key (get yours at https://portal.jup.ag)
VITE_JUPITER_API_KEY=your-api-key-here

# Tapestry API (server-side only; do NOT prefix with VITE_)
# Used by /api/tapestry/* proxy routes
TAPESTRY_API_KEY=
# Optional; defaults to https://api.usetapestry.dev/api/v1
TAPESTRY_API_URL=

# Firebase (chat)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_DATABASE_URL=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# ─── Mode-specific files (create these): ───
# .env.devnet:
#   VITE_NETWORK=devnet
#   VITE_RPC_URL=
#   VITE_ENABLE_TAPESTRY_SOCIAL=true
#
# .env.mainnet:
#   VITE_NETWORK=mainnet
#   VITE_RPC_URL=<your-mainnet-rpc>
#   VITE_PROGRAM_ID=<mainnet-program-id>
#   VITE_TREASURY_ATA=<mainnet-treasury-ata>
#   VITE_ADMIN_PUBKEY=<mainnet-admin-pubkey>
#   VITE_ENABLE_TAPESTRY_SOCIAL=true
#
# If running `vercel dev` locally for /api/tapestry/* routes:
#   put TAPESTRY_API_KEY in .env (shared) or export it in the shell before запуском
#   VITE_ENABLE_TAPESTRY_SOCIAL controls frontend rendering only (build-time flag)
